@page "/streams/{GameName}"

@using SmolStream.BlazorApp.Data
@using SmolStream.BlazorApp.Services
@inject StreamDataService StreamDataService

<h1>Top 100 Games on Twitch</h1>

<p>This component demonstrates fetching data from the Twitch API Games endpoint.</p>

@if (streams == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Game (toString)</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var s in streams)
            {
                <tr>
                    <td><img src="@g.BoxArtUrl" /></td>
                    <td>@g.ToString()</td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    [Parameter]
    public string GameName { get; set; }
    Stream[] streams;

    protected override async Task OnInitializedAsync()
    {
        games = await StreamDataService.GetStreamDataAsync();
    }
}
